name: Run Tests

on:
  push:
    branches: [ main, develop, dev-playground ]
  pull_request:
    branches: [ main, develop, dev-playground ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    env:
      AWS_ACCESS_KEY_ID: testing
      AWS_SECRET_ACCESS_KEY: testing
      AWS_DEFAULT_REGION: us-east-1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false  # prevent frontend submodule error

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov flake8 mypy

      - name: Lint with flake8
        run: |
          flake8 backend --max-line-length=120 --ignore=E203,W503

      - name: Type-check with mypy
        run: |
          mypy backend

      - name: Run pytest (quick check)
        run: pytest -q --maxfail=1 --disable-warnings

      - name: Run pytest with coverage
        run: pytest --cov=backend/services --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}